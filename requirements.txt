# requirements.txt
disnake[voice]>=2.9
# Voice encryption.  Comes in via disnake's [voice] extra, but named explicitly
# so an install without the extra fails at install time, not at !join.
PyNaCl>=1.5,<1.7
dave.py>=0.1.0
PyQt6>=6.6
numpy>=1.26
requests>=2.31
python-dotenv>=1.0
certifi>=2024.2.2
